{
  "name": "hermes-remotion-render-helper",
  "private": true,
  "description": "Thin Node wrapper around @remotion/lambda's renderMediaOnLambda / getRenderProgress, invoked by hermes/integrations/remotion_client.py via subprocess. Kept separate from the Python service's requirements.txt since Remotion's stable interface is its own npm SDK, not a documented REST API.",
  "type": "module",
  "dependencies": {
    "@remotion/lambda": "^4.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "remotion": "^4.0.0",
    "zod": "4.3.6"
  }
}
